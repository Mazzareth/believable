package app.masterwork.believable.classes.warden;

import app.masterwork.believable.character.BasicClass;

import java.util.List;

public final class WardenClass {
    public static final BasicClass DEFINITION = new BasicClass(
        "Warden",
        "Dedicate your life to preparation magic. Shape the rules of a region, warding spawns and stabilizing hostile lands.",
        List.of(),
        List.of()
    );

    private WardenClass() {
    }
}
